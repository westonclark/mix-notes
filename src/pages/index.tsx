import { type NextPage } from "next";
import { SignIn, useUser } from "@clerk/nextjs";
import Head from "next/head";
import Link from "next/link";
import { api } from "~/utils/api";

import {
  ClerkProvider,
  SignedIn,
  SignedOut,
  SignInButton,
  UserButton,
} from "@clerk/nextjs";

const Home: NextPage = () => {
  const { data } = api.projects.getAll.useQuery();

  const user = useUser();
  console.log();
  return (
    <>
      <Head>
        <title>Mix Notes</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <header className="flex justify-end bg-neutral-950 p-6 text-white">
        <SignedIn>
          {/* Mount the UserButton component */}
          <UserButton afterSignOutUrl="/" />
        </SignedIn>
        <SignedOut>
          {/* Signed out users get sign in button */}
          <SignInButton />
        </SignedOut>
      </header>
      <main className="flex min-h-screen flex-col items-center justify-center bg-neutral-950 text-white">
        <div>
          {data?.map((project) => (
            <div key={project.id}>{project.name}</div>
          ))}
        </div>
      </main>
    </>
  );
};

export default Home;
